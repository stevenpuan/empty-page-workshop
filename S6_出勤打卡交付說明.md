# S6 出勤打卡 — 交付說明

## 後端交付物

| 項目 | 說明 |
|------|------|
| migration | `010_attendance.sql`（11 sections） |
| 新 table | `attendances`、`attendance_amendments` |
| 新 function | `haversine_m(lat1,lng1,lat2,lng2)` — Haversine 距離（公尺） |
| 新 RPC | `clock_in(p_lat, p_lng)` — 上班打卡 |
| 新 RPC | `clock_out(p_lat, p_lng)` — 下班打卡 |
| 新 RPC | `approve_amendment(p_amendment_id, p_approved, p_note)` — 核准/駁回補卡 |
| 新 view | `attendance_monthly_summary` — 月出勤彙總（記帳士 Excel） |
| 新 view | `attendance_daily_report` — 每日出勤明細（前端列表） |
| RLS | attendances 4 policies、attendance_amendments 4 policies |
| triggers | `set_attendances_updated_at`、`set_amendments_updated_at` |

### 驗證結果

```
15/15 PASS
1. haversine_m function          PASS
2. attendances table             PASS
3. attendance_amendments table   PASS
4. attendances RLS enabled       PASS
5. amendments RLS enabled        PASS
6. attendances policies (4)      PASS
7. amendments policies (4)       PASS
8. clock_in() RPC                PASS
9. clock_out() RPC               PASS
10. approve_amendment() RPC      PASS
11. attendance_monthly_summary   PASS
12. attendance_daily_report      PASS
13. attendances updated_at trigger PASS
14. amendments updated_at trigger  PASS
15. unique constraint            PASS
```

## 資料表設計摘要

### attendances（出勤紀錄）
- `company_id` + RLS 雙層隔離
- `employee_id` + `work_date` UNIQUE — 一人一天一筆
- 上班：`clock_in_at / lat / lng / distance_m`
- 下班：`clock_out_at / lat / lng / distance_m`
- 狀態：`normal / late / early_leave / missing / absent / off`
- 遲到分鐘：`late_minutes`、早退分鐘：`early_leave_minutes`
- 工時：`work_minutes`（已扣休息）
- 加班：`ot_minutes_134`（前2h ×1.34）、`ot_minutes_167`（2h+ ×1.67）、`ot_minutes_holiday`
- 異常：`is_abnormal_location` + `abnormal_reason`

### attendance_amendments（補卡申請）
- `request_type`：`clock_in / clock_out / both`
- `status`：`pending → approved / rejected`
- 核准後由 `approve_amendment()` 自動回寫 attendances

### RPC 邏輯重點
- **clock_in**：後端用 `haversine_m()` 重算距離 → 比對 `companies.clock_radius_m` → 超距標記異常；比對 `shifts.start_time + late_tolerance_minutes` → 判遲到
- **clock_out**：計算 `work_minutes = 總時長 - break_minutes`；超標準工時 → 前120分鐘存 `ot_134`、其餘存 `ot_167`
- **approve_amendment**：manager only；核准時自動建立/更新 attendance，回寫打卡時間與工時

---

## 前端 Lovable 指令

### 指令 O — 打卡頁 `/clock`

```
請建立打卡頁面 /clock，這是員工每日上下班打卡的核心頁面。

## 頁面結構

頂部顯示：
- 目前時間（即時更新，每秒刷新，格式 HH:mm:ss）
- 今日日期（YYYY/MM/DD 星期X）
- 員工姓名與班別名稱

中間大按鈕區：
- 如果今日尚未打上班卡 → 顯示「上班打卡」大圓形按鈕（綠色）
- 如果已打上班卡但未打下班卡 → 顯示「下班打卡」大圓形按鈕（橘色）
- 如果上下班都打完 → 顯示「今日已完成打卡」（灰色，不可點）

下方顯示今日打卡資訊卡片：
- 上班時間 + 距離（公尺）+ 是否異常
- 下班時間 + 距離（公尺）+ 是否異常
- 狀態 badge（normal=綠/late=紅/early_leave=橘/missing=灰）
- 工時、加班時數

## 打卡流程

1. 按下按鈕後，先用 navigator.geolocation.getCurrentPosition 取得 GPS
2. 顯示 loading spinner + "定位中..."
3. 取得座標後，呼叫 RPC：
   - 上班：`supabase.rpc('clock_in', { p_lat: lat, p_lng: lng })`
   - 下班：`supabase.rpc('clock_out', { p_lat: lat, p_lng: lng })`
4. 成功後 toast 顯示結果，刷新頁面資料
5. 如果 GPS 取得失敗，顯示錯誤提示但仍允許打卡（傳 null）

## 資料來源

- 今日出勤：`supabase.from('attendances').select('*').eq('employee_id', empId).eq('work_date', today).maybeSingle()`
- 員工班別：從 currentEmployee 的 shift_id 關聯 shifts 取 name、start_time、end_time

## 補卡入口

底部放一個「申請補卡」連結按鈕，點擊後導向 /clock/amendment（下一個指令建立）

## 設計風格

打卡按鈕要大且明顯（至少 150px 圓形），讓工廠現場員工容易操作。
時間顯示要大字體。整體風格簡潔，減少干擾。
```

### 指令 P — 補卡申請頁 `/clock/amendment`

```
請建立補卡申請頁面 /clock/amendment。

## 頁面功能

員工忘記打卡或打卡異常時，可以提交補卡申請。

## 表單欄位

- 補卡類型（request_type）：下拉選單
  - clock_in（補上班卡）
  - clock_out（補下班卡）
  - both（補上下班卡）
- 補打時間（request_time）：datetime picker — 上班卡時間
- 補打下班時間（request_time_out）：datetime picker — 僅 type=both 時顯示
- 原因（reason）：textarea，必填

## 提交邏輯

```js
const { error } = await supabase
  .from('attendance_amendments')
  .insert({
    company_id: currentCompanyId,
    employee_id: currentEmployeeId,
    request_type: formData.requestType,
    request_time: formData.requestTime,
    request_time_out: formData.requestType === 'both' ? formData.requestTimeOut : null,
    reason: formData.reason,
    status: 'pending',
    created_by: user.id,
    updated_by: user.id
  });
```

## 我的申請紀錄

表單下方顯示「我的補卡申請」列表：
- 查詢：`supabase.from('attendance_amendments').select('*').eq('employee_id', empId).order('created_at', { ascending: false })`
- 顯示：申請日期、類型、補打時間、原因、狀態 badge（pending=黃/approved=綠/rejected=紅）、核准人備註

## 導航

頂部返回按鈕回到 /clock
```

### 指令 Q — 出勤管理頁 `/attendance`（manager/owner）

```
請建立出勤管理頁面 /attendance，僅 manager 和 owner 角色可見。

## 頁面結構

### 上方：日期篩選器
- 日期選擇器（預設今日）
- 可切換至日期範圍模式

### 主區域 Tab 切換

**Tab 1：每日出勤（預設）**

使用 attendance_daily_report view：
```js
const { data } = await supabase
  .from('attendance_daily_report')
  .select('*')
  .eq('work_date', selectedDate)
  .order('emp_no');
```

表格欄位：
| 工號 | 姓名 | 班別 | 上班打卡 | 下班打卡 | 狀態 | 遲到(分) | 早退(分) | 工時(分) | 加班 | 異常 | 補卡 |

- 狀態用 badge 顏色區分
- 異常欄：如果 is_abnormal_location = true，顯示 ⚠️ icon，hover 顯示原因
- 補卡欄：如果 pending_amendments > 0，顯示數字 badge + 連結到補卡審核
- 點擊某一列可展開詳細資訊（打卡座標、距離等）

**Tab 2：補卡審核**

查詢待審核補卡：
```js
const { data } = await supabase
  .from('attendance_amendments')
  .select('*, employee:employees(emp_no, name)')
  .eq('status', 'pending')
  .order('created_at');
```

每筆顯示：員工姓名、申請類型、補打時間、原因

操作按鈕：
- ✅ 核准：呼叫 `supabase.rpc('approve_amendment', { p_amendment_id: id, p_approved: true, p_note: note })`
- ❌ 駁回：彈出備註輸入框後呼叫 `supabase.rpc('approve_amendment', { p_amendment_id: id, p_approved: false, p_note: note })`

**Tab 3：月報表**

使用 attendance_monthly_summary view：
```js
const { data } = await supabase
  .from('attendance_monthly_summary')
  .select('*')
  .eq('month', selectedMonth)
  .order('emp_no');
```

表格欄位：
| 工號 | 姓名 | 出勤天數 | 正常 | 遲到 | 早退 | 缺勤 | 曠職 | 遲到總分鐘 | 早退總分鐘 | 總工時 | 加班134 | 加班167 | 假日加班 | 異常次數 |

加「匯出 Excel」按鈕，使用 xlsx 套件匯出。

## 權限控制

頁面載入時檢查角色：
```js
if (currentRole !== 'owner' && currentRole !== 'manager') {
  navigate('/clock');
  return;
}
```
```

### 指令 R — Sidebar 加入出勤選單

```
請在 sidebar 導航列中加入出勤相關選單項目：

## 新增選單項目

在現有選單的適當位置（建議在「看板」之後），加入：

1. **打卡** — icon: Clock, path: /clock
   - 所有角色都可見

2. **出勤管理** — icon: CalendarCheck, path: /attendance
   - 僅 owner 和 manager 角色可見

## 選單分組

如果 sidebar 有分組機制，將這兩項放在「出勤」群組下。
如果沒有分組，直接加在現有選單項目之後。

## active 狀態

當前路由匹配時，選單項目要有 active 高亮樣式。
/clock 和 /clock/amendment 都歸屬「打卡」選單。
/attendance 和 /attendance/* 都歸屬「出勤管理」選單。
```

---

## 前端指令投放順序

| 順序 | 代號 | 內容 | 路由 |
|------|------|------|------|
| 1 | O | 打卡頁 | `/clock` |
| 2 | P | 補卡申請頁 | `/clock/amendment` |
| 3 | Q | 出勤管理頁（manager） | `/attendance` |
| 4 | R | Sidebar 出勤選單 | — |

依 O → P → Q → R 順序送 Lovable。

---

## 驗收檢查項

- [ ] 打卡頁能取得 GPS 並呼叫 clock_in / clock_out
- [ ] 打卡後顯示距離、狀態、工時
- [ ] 異常距離正確標記
- [ ] 補卡申請可提交，列表可查看
- [ ] manager 可在出勤管理頁看到所有員工今日出勤
- [ ] 補卡審核的核准/駁回正確呼叫 approve_amendment
- [ ] 月報表數據正確，可匯出 Excel
- [ ] Sidebar 出勤選單正確顯示，權限控制正常
